---
import Layout from '../components/Layout.astro';
import { siteConfig, getImageSrcSet, imageSizes } from '../config.ts';
import { contactContent } from '../content/contact';
import * as LucideIcons from '@lucide/astro';
import { marked } from 'marked';

const contactGeneralHtml = await marked(contactContent.contactGeneral.text);
const contactStartHtml = await marked(contactContent.contactStart.text);

const getInitials = (name: string) =>
  name
    .split(' ')
    .filter(Boolean)
    .slice(0, 2)
    .map((part) => part[0])
    .join('')
    .toUpperCase();
---

<Layout title={`Kontakta oss - ${siteConfig.site.title}`} description={siteConfig.site.description}>
  <main class="min-h-screen">
    <div class="mx-auto max-w-7xl px-4 py-20 sm:px-6 lg:px-8">
      <header class="mt-12 mb-12 text-center">
        <h1 class="text-5xl font-bold mb-6">{contactContent.header}</h1>
      </header>

      <section class="mb-16">
        <div class="grid grid-cols-1 gap-6 lg:grid-cols-2">
          <div class="group block scroll-fade-up bg-[var(--color-bg-card)] border-mono-lighter rounded-2xl border p-8 stagger-1 hover:shadow-lg transition-shadow">
            <div class="flex items-start gap-4 mb-6">
              <div class="feature-icon bg-[var(--color-orange)] flex h-14 w-14 items-center justify-center rounded-lg text-white flex-shrink-0">
                {(() => { const IconComponent = LucideIcons[contactContent.contactGeneral.icon]; return <IconComponent class="h-7 w-7" />; })()}
              </div>
            </div>
            <div class="text-lg text-gray-700 leading-relaxed space-y-4 [&>p]:mb-4 [&>em]:italic [&>strong]:font-semibold" set:html={contactGeneralHtml}></div>
          </div>
          <div class="group block scroll-fade-up bg-[var(--color-bg-card)] border-mono-lighter rounded-2xl border p-8 stagger-2 hover:shadow-lg transition-shadow">
            <div class="flex items-start gap-4 mb-6">
              <div class="feature-icon bg-[var(--color-orange)] flex h-14 w-14 items-center justify-center rounded-lg text-white flex-shrink-0">
                {(() => { const IconComponent = LucideIcons[contactContent.contactStart.icon]; return <IconComponent class="h-7 w-7" />; })()}
              </div>
            </div>
            <div class="text-lg text-gray-700 leading-relaxed space-y-4 [&>p]:mb-4 [&>em]:italic [&>strong]:font-semibold" set:html={contactStartHtml}></div>
          </div>
        </div>
      </section>

      <section class="mb-16">
        <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4">
          {contactContent.people.map((person, index) => (
            <article class={`group block scroll-fade-up bg-[var(--color-bg-card)] border-mono-lighter rounded-2xl border p-6 stagger-${(index % 9) + 1} hover:shadow-lg transition-shadow`}>
              <div class="mb-5">
                <div class="relative aspect-[4/5] overflow-hidden rounded-xl bg-gray-100">
                  <img
                    srcset={getImageSrcSet(person.photo)}
                    sizes={imageSizes.contactPhoto}
                    src={person.photo.desktop}
                    alt={`Foto på ${person.name}`}
                    class="h-full w-full object-cover"
                    loading="lazy"
                    onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"
                  />
                  <div class="absolute inset-0 hidden items-center justify-center bg-gray-100 text-3xl font-semibold text-mono-dark">
                    {getInitials(person.name)}
                  </div>
                </div>
              </div>
              <h3 class="text-xl font-semibold text-mono-black mb-1 card-title">{person.name}</h3>
              <p class="text-mono-dark mb-4">{person.title}</p>
              <div class="space-y-2 text-sm">
                <div>
                  <p class="text-mono-gray">Telefon</p>
                  <a
                    href={`tel:${person.phoneNumber}`}
                    class="text-mono-dark hover:text-mono-black"
                  >
                    {person.phoneDisplay}
                  </a>
                </div>
                <div>
                  <p class="text-mono-gray">E-post</p>
                  <a
                    href={`mailto:${person.email}`}
                    class="text-mono-dark hover:text-mono-black break-all"
                  >
                    {person.email}
                  </a>
                </div>
              </div>
            </article>
          ))}
        </div>
      </section>
      
      <section>
        <div class="group block scroll-fade-up bg-[var(--color-bg-card)] border-mono-lighter rounded-2xl border p-8 md:p-10 hover:shadow-lg transition-shadow">
          <h2 class="text-2xl font-semibold mb-6 card-title">Bolagsuppgifter</h2>
          <div class="grid grid-cols-1 gap-6 md:grid-cols-3">
            <div>
              <p class="text-sm uppercase text-mono-gray">Bolag</p>
              <p class="text-lg font-semibold text-mono-black">
                {contactContent.company.name}
              </p>
            </div>
            <div>
              <p class="text-sm uppercase text-mono-gray">Org.nr</p>
              <p class="text-lg font-semibold text-mono-black">
                {contactContent.company.orgNumber}
              </p>
            </div>
            <div>
              <p class="text-sm uppercase text-mono-gray">Bankgiro</p>
              <p class="text-lg font-semibold text-mono-black">
                {contactContent.company.bankgiro}
              </p>
            </div>
          </div>
        </div>
      </section>

    </div>
  </main>
</Layout>
